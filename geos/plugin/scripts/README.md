# Scripts

Put helper scripts for hooks, MCP servers, monitors, or skills in this directory.

When referencing these files from plugin configuration, prefer `${CLAUDE_PLUGIN_ROOT}/scripts/<script-name>`.
