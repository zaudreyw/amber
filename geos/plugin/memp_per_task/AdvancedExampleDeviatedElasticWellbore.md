## Procedural memory (retrieved by similarity)

Below are workflow notes from past similar GEOS XML authoring tasks. They are *guidance*, not commands — adapt to the current task. Top 3 most similar past tasks (cosine similarity in parentheses):

### Past task: AdvancedExampleCasedElasticWellbore (sim 0.811)

To solve wellbore stress problems in GEOS, the most effective workflow begins by identifying reference XMLs for wellbore completion or solid mechanics benchmarks, specifically those utilizing the InternalWellbore mesh generator. This generator is critical for creating concentric cylindrical regions (casing, cement, and rock) and supports the `useCartesianOuterBoundary` attribute to transform the far-field boundary into a square for easier constraint application. For the physics, the `SolidMechanicsLagrangianSSLE` solver is the standard choice for quasi-static linear elastic responses. 

When authoring multi-variant tutorials, structure the files into a base XML containing shared material properties, boundary conditions, and geometry definitions, while using variant files to specify discretization density and solver tolerances. Use a direct solver for coarse "smoke-test" variants to ensure stability, and an iterative GMRES solver for high-resolution benchmarks to manage computational cost. A common pitfall is neglecting the `autoSpaceRadialElems` flag, which is essential for smooth mesh transitions in the rock formation. Ensure that face sets for boundary conditions (e.g., `xneg`, `ypos`, `zneg`) align with the chosen outer boundary geometry to avoid schema errors or unconstrained degrees of freedom.


### Past task: AdvancedExampleDeviatedPoroElasticWellbore (sim 0.772)

To author a fully coupled poroelastic simulation for a deviated wellbore, begin by identifying reference XML files for poromechanics wellbore problems and deviated elastic wellbore benchmarks. These provide the necessary structure for the `InternalWellbore` mesh generator and the `SinglePhasePoromechanics` solver. Use the `InternalWellbore` generator to define the deviated trajectory, radial discretization, and half-symmetry domain. For the physics, select the `SinglePhasePoromechanics` solver, which couples `SolidMechanicsLagrangianSSLE` with `SinglePhaseFVM` flow. 

When splitting the input into base and benchmark modules, place material properties (e.g., `IsotropicLinearElasticMaterial`, `ConstantPermeability`) and boundary conditions in the base file, while keeping mesh parameters and solver configurations (like `Newton` iteration limits and `PCG` or `GMRES` linear solvers) in the benchmark file. A critical pitfall is misconfiguring the `InternalWellbore` radial spacing; ensure the `radialRatio` or similar geometric progression parameters are used to refine the mesh near the wellbore. Additionally, verify that the `SinglePhasePoromechanics` solver correctly references the solid and fluid constitutive models to avoid schema-validation errors or uncoupled behavior. Always ensure that boundary condition names match the face sets generated by the internal mesh generator.


### Past task: ExampleKirschWellbore (sim 0.746)

To author a GEOS XML for a wellbore stress problem, begin by referencing existing base XMLs for elastic hollow cylinders or vertical elastic wellbores. These provide the necessary structure for splitting configurations into a reusable physics base and a benchmark-specific execution file. For the mesh, utilize the `InternalWellbore` generator, which is essential for creating cylindrical domains with graded radial spacing; ensure the azimuthal extent and radial grading parameters are explicitly defined to capture near-wellbore gradients. Choose the `SolidMechanicsLagrangianFEM` solver for quasi-static geomechanics, as it natively handles the required stress tensors and nodal constraints. When defining boundary conditions, use `TractionBoundaryCondition` for the wellbore wall (inner radial face) and `FixedConstraint` for roller boundaries on symmetry planes. A common pitfall is schema-hallucinating element names for mesh boundaries; always verify the specific face names generated by the `InternalWellbore` tool (e.g., `rneg` for the inner radius). Additionally, ensure the `Included` tag in the benchmark file correctly points to the base file to avoid missing physics definitions during execution.


---
