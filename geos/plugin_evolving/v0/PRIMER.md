# GEOS

GEOS is an open-source multiphysics simulator. Tasks require authoring an XML input file (or a small set of XMLs cross-referenced via `<Included>`).

- The GEOS source repository is mounted at `/geos_lib/`.
- Write your final XML output to `/workspace/inputs/`.
