# Decisions

Major methodological decisions live here as `D-NNN_slug.md` files. Each has YAML frontmatter with `id`, `title`, `date`, `dag_nodes`, and optional typed `links`.

Created by `/decision-log` or manually for major pivots.
