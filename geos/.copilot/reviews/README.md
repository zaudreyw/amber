# Reviews

Persistent review notes from subagents and adversarial-review runs live here as `RN-NNN_<source>_<slug>.md`. Source is typically `reviewer`, `designer`, or `adversarial`.

Each has YAML frontmatter with `id`, `title`, `date`, `source`, `dag_nodes`, and optional `links`.
